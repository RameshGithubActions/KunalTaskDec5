name: Workflow Run 1
on:
  workflow_run:
    workflows: [Global Variables Example]
    types: [completed]
jobs:
  echo-string:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Global Variables Completed Check Again"
          WORKFLOW_RUN_VARIABLE=123
          echo "Workflow Run Variable: $WORKFLOW_RUN_VARIABLE"
